<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;

//class Packages
//{
    /**
     * Handle an incoming request.
     *
     * @param  \Illuminate\Http\Request  $request
     * @param  \Closure(\Illuminate\Http\Request): (\Illuminate\Http\Response|\Illuminate\Http\RedirectResponse)  $next
     * @return \Illuminate\Http\Response|\Illuminate\Http\RedirectResponse
     */
   // public function handle(Request $request, Closure $next)
   // {
      
               // $user = $request->user();
        
               // if ($user && $user->role === 'subscriber') {
                  
                  //  if ($user->products()->count() >= 50) {
                       // return response()->json(['message' => 'You have reached the maximum product upload limit of 50 products.'], 403);
                    //}
                //} elseif ($user && $user->role === 'subscriber_basic') {
                   
                   // if ($user->products()->count() >= 100) {
                       // return response()->json(['message' => 'You have reached the maximum product upload limit of 100 products.'], 403);
                   // }
                //} elseif ($user && $user->role === 'subscriber_premium') {
                    
                  //  if ($user->products()->count() >= 200) {
                       // return response()->json(['message' => 'You have reached the maximum product upload limit of 200 products.'], 403);
                   // }
                //}
        
               // return $next($request);
            //}
       // }
  